# API gateway base URL (backend must be reachable here)
VITE_API_BASE_URL=http://localhost:3000

# Sentry browser DSN. Leave empty in development to disable.
VITE_SENTRY_DSN=

# Environment tag (development | staging | production). Used by Sentry and dev-only UI such as /styleguide.
VITE_ENV=development
